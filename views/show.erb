<div class="show-container">

  <div class="row">

    <div class="col-md-12">
      <!-- Heading -->
      <h3><%= @post.heading %></h3>
      <!-- Date -->
      <p><%= @post.postdate %></p>

      <p>By <%= @post.user.name if @post.user %> </p>

      <div class="image-container"> <img src="<%= @post.image %>" alt="" class=".img-responsive"> </div>

      <p><%= @post.post %></p>

  <% if logged_in? && current_user.email == "yang.j.li91@gmail.com" || logged_in? && current_user.id == @post.user.id %>
    <form action="/posts/<%= @post.id %>/edit">
      <div class="col-sm-4">
        <button class="btn btn-lg btn-primary btn-block" type="submit">Edit</button>
      </div>
    </form>

    <form action="/posts/<%= @post.id %>" method="post">
      <input type="hidden" name="_method" value="delete">
      <div class="col-sm-4">
        <button class="btn btn-lg btn-primary btn-block" type="submit">Delete</button>
      </div>
    </form>

  <% end %>

  <% if logged_in? && current_user.email == "yang.j.li91@gmail.com" %>
    <form action="/posts/<%= @post.id %>/edit/">
      <div class="col-sm-4">
        <button class="btn btn-lg btn-primary btn-block" type="submit">Feature</button>
      </div>
    </form>
  <% end %>


    </div>

  </div>

</div>
